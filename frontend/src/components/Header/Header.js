import styled from "styled-components";

const HeaderText = styled.h1`
    font-size: 1.2em;
`;

const Header = (props) => {
    return <HeaderText>English to Italian</HeaderText>;
};

export default Header;